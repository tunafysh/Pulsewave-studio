export default function Convert() {
    return (
        <main className="p-4" >
            <h1 className="text-2xl text-primary font-bold">Convert</h1>
            <div id="desc">
            <div className="prose dark:prose-invert">
            <p className="text-sm">Pulsewave uses its own format to stream music instead of MP3, Vorbis and others. It uses the .plw file extension. Since the Pulsewave format is new and arbitrary, Pulsewave allows you to convert well known formats like .mp3 and others to .plw.</p>
            </div>
            </div>
        </main>
    )
}